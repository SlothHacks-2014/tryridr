require('derby').run __dirname + '/src/server/index.coffee'
#fs = require 'fs'
#myServer = require __dirname + '/src/server/index.coffee'
#https = require 'https'

#options = 
# key: fs.readFileSync './key.pem'
# cert: fs.readFileSync './cert.pem'

#https.createServer(options, myServer).listen(3000)
